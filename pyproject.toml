[project]
name = "adminer-teleport"
version = "0.1.0"
description = "Access teleport proxy database using adminer (container)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
]
